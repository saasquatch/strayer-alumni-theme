<td>
  {{#if referral.imageUrl}}
    <img class="img-circle squatch-referrals-avatar" src="{{referral.imageUrl}}">
  {{else}}
    <img class="img-circle squatch-referrals-avatar" src="{{gravatar referral.email default="mm" rating="pg" size=40 secure=isSecureUrl}}">
  {{/if}}

  <div class="squatch-referrals-heading">{{stringEmptyHandler referral.firstName onEmpty="Anonymous"}}</div>

  <div class="squatch-referrals-description">
    <span class="hidden-sm">
      {{#if description}}
        {{description}}
      {{else}}
        {{#if paid}}
          Paid User
        {{else}}
          Trial User
        {{/if}}
      {{/if}}
    </span>

    <span class="hidden-md
      {{#if paid}}
        text-green
      {{else}}
        text-yellow
      {{/if}}
    ">
      {{#if paid}}
        {{#if reward.value}}
          ${{math reward.value ' / 100'}}
        {{else}}
          {{#if referred}}
            {{format-reward 'REFERRED' includeUnit=true}}
          {{else}}
            {{format-reward 'REFERRER' includeUnit=true}}
          {{/if}}
        {{/if}}
      {{else}}
        {{#if referred}}
          {{format-reward 'REFERRED' includeUnit=true}} Pending
        {{else}}
          {{format-reward 'REFERRER' includeUnit=true}} Pending
        {{/if}}
      {{/if}}
    </span>
  </div>
</td>

<td class="hidden-sm">
  <div class="squatch-referrals-heading">
    {{#if heading}}
      {{heading}}
    {{else}}
      {{#if paid}}
        Signed Up
      {{else}}
        Referred
      {{/if}}
    {{/if}}
  </div>

  <div class="squatch-referrals-description" data-moment="true">{{dateReferred}}</div>
</td>
<td>
  <i class="icon squatch-referrals-icon
    {{#if paid}}
      icon-ok-circled text-green
    {{else}}
      icon-attention text-yellow
    {{/if}}
  "></i>

  <div class="squatch-referrals-heading hidden-sm">
    {{#if paid}}
      {{#if reward.value}}
        {{#if (stringCompare reward.unit "MONTH" emptyOnFalse=true)}}
          {{reward.value}} Month<span class="hidden-{{math value '== 1'}}">s</span>
        {{else}}
          ${{math reward.value ' / 100'}}
        {{/if}}
      {{else}}
        {{#if referred}}
          {{format-reward 'REFERRED' includeUnit=true}}
        {{else}}
          {{format-reward 'REFERRER' includeUnit=true}}
        {{/if}}
      {{/if}}

      {{#if reward.fuelTankCode}}
        Free Credit
      {{/if}}
    {{else}}
      Trial Signup
    {{/if}}
  </div>

  <div class="squatch-referrals-description hidden-sm
    {{#if paid}}
      text-green
    {{else}}
      text-yellow
    {{/if}}
  ">
    {{#if reward.fuelTankCode}}
      CODE: {{reward.fuelTankCode}}
    {{else}}
      {{#if paid}}
        Free Credit
      {{else}}
        Credit Pending
      {{/if}}
    {{/if}}
  </div>
</td>
